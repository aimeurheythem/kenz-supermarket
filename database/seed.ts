/**
 * Seed initial data for development and first-time setup.
 * Currently a no-op — the Setup Wizard handles all initial data creation.
 */

export async function seedDatabase(): Promise<void> {
    // Nothing to seed. The onboarding wizard creates the admin user,
    // and the store owner adds their own products, categories, and suppliers.
}
